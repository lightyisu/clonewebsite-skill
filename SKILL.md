---
name: replicate-website
description: 1:1 复刻一个真实网站的页面(风格、结构、文案、交互全部照抄)。当用户要求"复刻/仿/克隆/做一个像 XX 官网一样的页面"或指定模仿某个网站(如 cursor.com、linear.app)时使用。核心方法:抓真值 → 照抄骨架 → 双页同尺寸对比 → 只改差距 → 再截图验证。
when_to_use: 用户要求复刻、模仿、克隆某个真实网站页面,或嫌弃产出"AI 味重"要求贴近原网站时
argument-hint: <目标网站 URL 或名称>
---

# 网站复刻

## 核心心法

**不要"设计"一个像它的页面,要"抄"一个它的页面。** 凭印象写 = AI 味的来源。核心循环:抓真值 → 照抄骨架 → 对比差距 → 只改差距 → 再截图验证。

## 硬规则(违反即返工)

1. **只做原网页上存在的组件元素。** 不发明原网页没有的区块、卡片、按钮、装饰。拿不准原页有没有,就回快照/截图里确认,确认没有就不做。
2. **文案 1:1 抄原站真实文案**(含句式、长度、大小写、标点)。禁止自己写营销话术。原站是英文就保持英文,不翻译。
3. **设计参数照抄,不自己发明。** 颜色、字号、字距、圆角、阴影一律来自第 1 步的 DESIGN.md。
4. 禁止事项以 DESIGN.md 的 Don't 清单为准(如纯黑白、标题加粗、渐变、非指定圆角)。

## 流程

### 第 1 步:拉取设计规范(图纸)

- 在 styles.refero.design 找目标网站的风格档案页。
- WebFetch 只能拿到摘要;**用浏览器打开页面、点 DESIGN.md / CSS Variables 标签,抓完整 token**(色板、字号阶梯、字距、圆角、阴影公式、组件规格、Do/Don't)。
- 整理成本地 `DESIGN.md` 备查。格式示例见本 skill 的 `references/design-md-example-cursor.md`(Cursor 官网完整规范)。
- Refero 没收录的网站:用 Chrome DevTools 的 `get_css_styles` / `evaluate_script` 直接从原站计算样式里提取同等内容。

### 第 2 步:抓真实结构与文案(骨架)⭐

> **第一次质变。** 真实结构必须逐屏抓取,不能靠猜。

- 用浏览器 MCP 打开目标网站,`take_snapshot` 拿可访问性树——它给出准确的区块顺序、真实文案、每个组件里的字段。
- 逐屏滚动 + 截图,记录:每个区块的排版方向(左对齐还是居中)、文案句式和长度、mockup 里的真实细节(时间戳、diff 数字、按钮文案)。
- **这份快照同时就是组件清单**:复刻产物的组件集合必须与它一致,多一个少一个都要对齐。

### 第 3 步:第一版实现(照抄骨架,套用规范)

- 单文件 HTML,把 DESIGN.md 的 CSS 变量原样放进 `:root`。
- 区块顺序、文案、布局方向 1:1 照抄第 2 步抓到的内容,不自由发挥。

### 第 4 步:双页同尺寸对比(找茬)⭐

> **第二次质变。** 把真假两个页面并排放,逐屏找茬,差距会自己浮出来。

- 用 Chrome DevTools MCP 同时打开目标网站和本地文件,**两边都 resize 成同一尺寸**(如 1440×900)。
- 逐屏截图对比,差异归三类:**布局差异**(对齐方向、区块组合、字号量级)/ **组件差异**(缺了哪些面板、真实感细节)/ **交互差异**(下拉、Tab、复制、hover)。
- 典型发现:导航吸顶、按钮是药丸形、中文衬线英文无衬线、Hero 左对齐不是居中、引言是三列卡片网格。

### 第 5 步:按差异清单重写(交互补齐)⭐

> **决定"像不像"的一步。** 静态复刻 70 分,交互补上 90 分。

- 逐项修,每修一批就重新截图验证一次,不要攒到最后。
- 交互用少量原生 JS:下拉点击换选中、列表点击高亮、复制按钮(clipboard + ✓ 反馈)、hover 态(150ms cubic-bezier(0.4,0,0.2,1))。只做原站有对应行为的交互。
- 真实感细节优先补:时间戳(now/10m/30m)、diff 数字(+52 -0 绿红配色)、"思考 6 秒"这类过程步骤。

### 第 6 步:背景画与字体(气质层)

- 风景/抽象背景用文生图生成,提示词锁定:低饱和、跟随原站色调、matte 笔触、无文字、横向构图。
- 字体:闭源字体用官方推荐的替代(如 CursorGothic → Inter);**中文按原站实际渲染走**(如 cursor.com 中文走衬线 `Noto Serif SC`,英文无衬线);代码用系统等宽;Google Fonts 一行引入。

## 工具清单

- Refero Styles(styles.refero.design):设计规范来源
- 浏览器 MCP(browser-use / Chrome DevTools):快照、截图、resize、evaluate_script
- Chrome DevTools 的 `take_screenshot` + `resize_page` 是对比环节主力
- 文生图:风格背景画

## Resources

- `references/design-md-example-cursor.md` — 一份完整 DESIGN.md 长什么样(Cursor 官网实测提取),含色板表、字号阶梯、阴影公式、12 个组件规格、16 条 Do/Don't、可直接粘贴的 CSS 变量块。
